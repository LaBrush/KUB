{% extends "::base.html.twig" %}

{% block body %}

	
		<article class="article">
			<h2 class="section-title">Modifier {{ groupe.name }}</h2>
		</article>

		<article class="article">
			{{ form_start(form) }}

				{% block form %}
				{% endblock %}

				<input type="submit">
			{{ form_end(form) }}
		</article>

		<article class="article">
			
			<a href="{{ path("groupe_delete", { id: groupe.id }) }}">...ou supprimer {{ groupe.name }}</a>

		</article>
		
			
{% endblock %}