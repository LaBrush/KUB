{% block form %}

	<span class="name">
		{{ form_errors(form.name) }}
		{{ form_label(form.name) }} <div class="input">{{ form_widget(form.name) }}</div>
	</span>

	<span class="type">
		{{ form_errors(form.type) }}
		Logiciel <div class="input">{{ form_widget(form.type) }}</div>
	</span>

{% endblock %}