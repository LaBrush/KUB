{% extends 'KubCollaborationBundle::layout.html.twig' %}

{% block content %}
	
	<article class="article">
		
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<input type="submit">
		{{ form_end(form) }}
		
	</article>

	<article class="article">
		
		<p>ou <a href="{{ path('kub_collaboration_organisation_delete', { projet_slug: projet.slug, tache_slug: tache.slug }) }}">Supprimer</a></p>

	</article>

{% endblock %}

{% block js %}
	
	<script type="text/javascript" src="{{ asset('ressources/select2/select2.min.js') }}"></script>
	<link rel="stylesheet" href="{{ asset('ressources/select2/select2.css') }}" />
	{{ form_javascript(form) }}
	{{ form_stylesheet(form) }}

{% endblock %}