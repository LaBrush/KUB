{% extends "::base.html.twig" %}

{% block title %}
	{{ parent() }} - Notes
{% endblock %}

{% block body %}

	<article class="article">
		<h2 class="section-title">Espace de notation</h2>
	</article>
    
	<article class="article">

		<label for="search-in-list">Chercher dans la liste :</label>
		<input id="search-in-list" type="text">

		<ul class="liste">
			{% for controle in liste_controles %}
			
				<a href="{{ path('kub_notes_professeur_edit', { id: controle.id }) }}">
					<li class="in-list li-list">
						<span>{{ controle }}</span>
					</li>
				</a>

			{% else %}
				<li>Vous n'avez enregistré aucun controle pour le moment</li>
			{% endfor %}
		</ul>
	</article>

	<article class="article">

		<label for="search-in-list">Chercher dans la liste :</label>
		<input id="search-in-list" type="text">

		<ul class="liste">
			{% for groupe in liste_groupes %}
			
				<a href="{{ path('kub_notes_professeur_noter_groupe', { groupe_id: groupe.id }) }}">
					<li class="in-list li-list">
						<span>{{ groupe }}</span>
					</li>
				</a>

			{% else %}
				<li>Vous n'avez cours avec aucun groupe pour le moment</li>
			{% endfor %}

		</ul>
	</article>

	<article class="article">

		<label for="search-in-list">Chercher dans la liste :</label>
		<input id="search-in-list" type="text">

		<ul class="liste">
			{% for cours in liste_cours %}
			
				<a href="{{ path('kub_notes_professeur_noter_cours', { cours: cours.id }) }}">
					<li class="in-list li-list">
						<span>{{ cours}}</span>
					</li>
				</a>

			{% else %}
				<li>Vous n'avez cours avez aucun groupe pour le moment</li>
			{% endfor %}
		</ul>
	</article>

{% endblock %}

{% block js %}
	<script type="text/javascript" src="{{ asset('ressources/jsmin/search.min.js') }}"></script>
	<script type="text/javascript" src="{{ asset('ressources/jsmin/fuse.min.js') }}"></script>
{% endblock %}